# Onyka — Docker Configuration (optional)
#
# No configuration is required to start Onyka.
# All secrets are auto-generated on first launch.
# Only set these if you need to customize something.

# Host port mapping (default: 3000)
# HOST_PORT=3000

# Public URL (required if using a custom domain)
# FRONTEND_URL=https://notes.example.com

# Reverse proxy (Nginx/Caddy)
# TRUST_PROXY=1

# Email (for password reset, 2FA, email verification)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=you@example.com
# SMTP_PASS=your-smtp-password
# SMTP_FROM=Onyka <noreply@example.com>
